# Dependencies
node_modules/

# Build output
dist/
# The baseline build a `verify-` script compares against (see the usage block in
# daemon/scripts/verify-mcp-runtime-validation.mjs).
dist-unfixed/
# The guard-removed copy verify-board-reconciler-guard.mjs builds to show the
# fleet being torn down. It removes it on exit; this is for the run that
# crashes before it can.
dist-guardless-*/

# Logs
*.log

# OS
.DS_Store
