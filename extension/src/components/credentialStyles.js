// The field styles the credential cards share, verbatim from the top of
// options.jsx where they lived while there was exactly one card. They moved
// here when a second card appeared so that the two cannot drift apart by
// someone editing one of them.

export const inputStyle = {
  width: '100%',
  padding: '10px',
  borderRadius: '6px',
  background: '#1e293b',
  border: '1px solid #334155',
  color: '#f8fafc',
  fontSize: '14px',
  boxSizing: 'border-box'
};

export const labelStyle = { display: 'block', fontWeight: 600, marginBottom: '8px' };
export const hintStyle = { fontSize: '13px', color: '#94a3b8', marginBottom: '12px' };
